# DevOpsWebhookGitJenkinsSlack
Custom webhook for github code merge,trigger jenkins job and send slack notification

#Prerequisites
#Install node version - 6.9.4
#Install npm version - 3.10.10

npm install;
